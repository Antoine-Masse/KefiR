% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pairwise.R
\name{pairwise}
\alias{pairwise}
\title{Automation function for pairwise calculations.}
\usage{
pairwise(
  x,
  g,
  type = "mean",
  alpha = 0.05,
  control = c(),
  pool.sd = FALSE,
  silent = TRUE,
  boot = FALSE,
  boot_type = mean,
  tr = 0.2,
  iter = 500,
  conf = 0.95,
  paired = FALSE,
  debug = FALSE
)
}
\arguments{
\item{x}{numerical vector}

\item{g}{category vector}

\item{type}{Character string indicating the type of test to perform. Options are:
\itemize{
\item "mean" for pairwise.t.test(p.adjust.method = "holm"),
\item "median" for pairwise.wilcox.test(p.adjust.method = "BH"),
\item "ks" for ks.test(),
\item "lincon" for lincon() from the \pkg{WRS2} package,
\item "dunn" for dunnTest() from the \pkg{FSA} package,
\item "BM" for brunner.munzel.test() from the \pkg{lawstat} package (independent samples),
\item "neminye" for frdAllPairsNemenyiTest() from the \pkg{PMCMRplus} package,
\item "boot": Establishes pairwise differences through a bootstrap approach applied to a descriptor (e.g., mean, median). The p-value is derived from the proportion of bootstrap samples where the descriptor difference is consistently positive or negative. Currently, \code{paired = TRUE} is not implemented for this option.
}}

\item{alpha}{threshold value of p-value to establish the groups.}

\item{control}{name of the category that will be used as a control to establish differences with '\emph{', '\strong{' and '}}'.}

\item{pool.sd}{switch to allow/disallow the use of a pooled SD.}

\item{silent}{for displaying or not warnings.}

\item{boot}{to activate the bootstrap on 'mean', 'median', 'lincon', 'BM' or 'dunn'.}

\item{boot_type}{Character string specifying the descriptor for \code{pairwise.boot}. Options are:
\itemize{
\item "meanbp" for the moving average,
\item "mean" for the mean,
\item "median" for the median,
\item "sd" for the standard deviation.
}}

\item{tr}{trimming parameter for lincon test (default: 0.2).}

\item{iter}{number of bootstrap iterations when boot=TRUE.}

\item{conf}{confidence level of bootstrap.}

\item{paired}{Logical. If TRUE, performs paired tests (e.g., paired t-tests or Wilcoxon signed-rank tests). Not compatible with 'ks', 'lincon', 'dunn', 'BM', or 'boot' types.}

\item{debug}{logical, if TRUE enables debug mode for troubleshooting.}
}
\value{
This function automates the work of the ks.test(), lincon() functions of \pkg{WRS2}, brunner.munzel.test() from \pkg{lawstat}, pairwise.t.test(), pairwise.wilcox.test(), dunnTest() from \pkg{FSA}, and frdAllPairsNemenyiTest() from \pkg{PMCMRplus}, and extracts groups of means or comparisons to a control with the catego() function.

It pre-sorts the means/medians to ensure that the groups are identified in ascending order.

It also identifies the robustness of these groups by establishing a bootstrap.
}
\description{
Automation function for pairwise calculations.
}
\examples{
data(iris)
pairwise(iris[,1], iris[,5], type = "mean") # t.test
pairwise(iris[,1], iris[,5], type = "median", alpha = 0.01, boot = TRUE) # wilcox
pairwise(iris[,1], iris[,5], type = "ks")
pairwise(iris[,1], iris[,5], type = "lincon")
pairwise(iris[,1], iris[,5], type = "dunn")
pairwise(iris[,1], iris[,5], type = "BM") # Brunner-Munzel
pairwise(iris[,1], iris[,5], type = "neminye")
}

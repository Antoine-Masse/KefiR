% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sys_posthoc_MANOVA.R
\name{.posthoc_MANOVA}
\alias{.posthoc_MANOVA}
\title{Post-hoc analyses for MANOVA}
\usage{
.posthoc_MANOVA(
  x = NULL,
  g = NULL,
  manova_result = NULL,
  alpha = 0.05,
  method = "both",
  verbose = FALSE,
  code = FALSE,
  debug = FALSE,
  k = NULL
)
}
\arguments{
\item{x}{Matrix of dependent variables (p columns = p DVs)}

\item{g}{Factor vector of group memberships}

\item{manova_result}{The MANOVA result object from .manova_analysis()}

\item{alpha}{Significance level (default = 0.05)}

\item{method}{Method for post-hoc: "discriminant" (default), "protected_anova", or "both"}

\item{verbose}{Logical, print detailed messages}

\item{code}{Logical. If TRUE, suppresses verbose messages for code-only mode (default: FALSE)}

\item{debug}{Logical, print debug messages}

\item{k}{Message counter}
}
\value{
List containing:
\itemize{
\item discriminant_analysis: Results from discriminant analysis (if requested)
\item protected_anovas: Results from protected univariate ANOVAs (if requested)
\item interpretation: Text interpretation of results
}
}
\description{
Performs appropriate follow-up analyses after a significant MANOVA result.
Includes discriminant analysis to identify which dependent variables
separate the groups, and protected univariate ANOVAs with Bonferroni correction.
}
\details{
\strong{APPROPRIATE POST-HOC METHODS FOR MANOVA}

After a significant MANOVA, standard univariate post-hocs (Tukey, etc.) are
inappropriate because they ignore the multivariate structure and correlations
between dependent variables.

\strong{Recommended approaches} (Huberty & Olejnik, 2006):
\enumerate{
\item \strong{Discriminant Analysis} (Primary recommendation)
\itemize{
\item Identifies which linear combination of DVs best separates groups
\item Shows which DVs contribute most to group separation
\item Provides canonical discriminant functions
\item Reference: Huberty & Olejnik (2006). Applied MANOVA and Discriminant Analysis, 2nd ed.
}
\item \strong{Protected Univariate ANOVAs}
\itemize{
\item Perform separate ANOVA on each DV
\item Apply Bonferroni correction: alpha_adjusted = alpha / p (p = number of DVs)
\item Only valid if MANOVA was significant (protection against Type I error inflation)
\item Reference: Stevens, J. P. (2009). Applied Multivariate Statistics for the Social Sciences, 5th ed.
}
\item \strong{Roy-Bargmann Stepdown Analysis} (Advanced, not implemented yet)
\itemize{
\item Tests DVs sequentially in order of theoretical importance
\item Controls for previously tested DVs
\item Reference: Roy, S. N., & Bargmann, R. E. (1958). Tests of multiple independence
}
}

\strong{NOT recommended:}
\itemize{
\item Pairwise multivariate comparisons (complex, low power)
\item Univariate post-hocs without MANOVA protection (inflates Type I error)
}
}
\note{
This function should only be called after a significant MANOVA result.
The MANOVA provides protection against Type I error inflation when
examining individual dependent variables.
}
\examples{
\dontrun{
# After significant MANOVA
manova_res <- .manova_analysis(cbind(A,B,C) ~ Group, data=data)
if (manova_res$test_statistics["Wilks", "p.value"] < 0.05) {
  posthoc <- .posthoc_MANOVA(
    x = manova_res$x,
    g = manova_res$g,
    manova_result = manova_res,
    method = "both",
    verbose = TRUE
  )
}
}
}
\references{
Huberty, C. J., & Olejnik, S. (2006). Applied MANOVA and Discriminant Analysis (2nd ed.). Wiley.
Stevens, J. P. (2009). Applied Multivariate Statistics for the Social Sciences (5th ed.). Routledge.
Tabachnick, B. G., & Fidell, L. S. (2019). Using Multivariate Statistics (7th ed.). Pearson.
}
\keyword{internal}
